package net.bluelotuscoding.skillleveling.mixin;

/**
 * Placeholder package for mixins if needed in the future.
 * The addon aims to use the core mod's APIs rather than mixins where possible.
 */
public class MixinPackageInfo {
    // This class exists only to ensure the mixin package directory is created
    // Mixins should only be used when the core mod's APIs don't provide needed functionality
}